# テスト計画書

> 文書バージョン: 2.1\
> 更新日: 2026-09-11  
> 対象フェーズ: Backend実装・Terraform構築・実API接続  
> 情報源方針: 実装時は最新の公式ドキュメントを最優先で再確認する。


## 1. Frontend
Vitest / Testing Library / Storybook / Playwright。現MSW版を実行する。
追加4コード・未知コードの日本語表示、Backend本文非表示をテスト単位のMSW上書きで確認する。
403/429のPOST自動再送なし、5xx・応答消失後の同一キー・Payload確認を検証する。
既存の下書き・未確定要求優先・評価再開・終端タイマー停止のテストを維持する。
lint・型・全テスト・本番／Mock／Storybookビルド・既存E2Eを実行する。

## 2. Backend
Unit:
- Validation
- UseCase
- DynamoDB repository
- Prompt builder
- Idempotency state transition（ユーザー・キー、Method・Path・Payload、保存応答再返却）
- 受付と非同期起動の整合・起動漏れ・重複・期限切れ回復
- Profile集計の二重加算防止
- Authorization

Integration:
- API Gateway相当event
- DynamoDB
- Cognito claims
- OpenAI Provider stub

## 3. Infrastructure
Terraform fmt / validate / plan。
devで主要Route Smoke Test。

## 4. Security
Unauthorized / Forbidden / cross-user access / log leakage / duplicate request。

## 5. 合格条件・既知問題

新規失敗がなく、更新文書の6 Route・ID・評価形式・入力制約・復旧仕様が正本と一致すること。
既知WebP寸法不一致は別途記録し、画像期待値を変更・スキップして隠さない。
実Backend／認証／AWSのテストは次工程。今回のMSW成功を実API接続完了と扱わない。
